# Test Environment Variables
NODE_ENV=test
PORT=10888

# Test Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hotel_booking_test
DB_USER=test_user
DB_PASS=test_password

# JWT Secret for testing
JWT_SECRET=test-jwt-secret-key

# File upload paths for testing
UPLOAD_DIR=./tests/fixtures/uploads
TEMP_DIR=./tests/fixtures/temp

# Log level for tests
LOG_LEVEL=error
